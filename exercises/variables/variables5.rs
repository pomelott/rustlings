// variables5.rs
// Execute `rustlings hint variables5` or use the `hint` watch subcommand for a hint.

fn main() {
    let number = String::from("T-H-R-E-E"); // don't change this line
    println!("Spell a Number : {}", number);
    let add_number = "F-O-U-R"; // don't rename this variable
    println!("Number plus two is : {}", number + add_number);
}
